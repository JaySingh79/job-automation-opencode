# Project Instructions & Setup

Use the virtual env for all python commands

## Virtual Environment & Dependencies
- Requirements file: [`requirements.txt`](file:///D:/Projects/PRACTICE/firecrawl_test/requirements.txt)
- Packages installed:
  - `python-dotenv`
  - `uv`
  - `pylint`
  - `crawl4ai`

## Package Management Rule
- **CRITICAL**: Always use `uv` for all Python package installations and environment operations (e.g., `uv pip install ...`, `uv venv`). Do not use raw `pip` directly.
